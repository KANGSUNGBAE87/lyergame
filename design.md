# 라이어게임 디자인 기준

작성일: 2026-06-07  
상태: 현재 구현 기준 통합 디자인 문서  
목적: 라이어게임을 "오프라인에서 더 빨리 시작되고, 더 덜 귀찮고, 결과가 더 재밌는 앱"으로 유지하기 위한 디자인 기준

## 1. 원본 자료

이 문서는 아래 자료를 하나로 묶은 프로젝트 루트 기준 문서다.

- `ai/plans/2026-06-06-liar-game-v2-design.md`
- `ai/plans/2026-06-07-design-review-redesign.md`
- `ai/plans/2026-06-07-fast-offline-play-design.md`
- 일반채팅 Figma 보드 로그: `/Users/kangsungbae/Documents/일반채팅/ai/session-logs/2026-06-07-figma-design-direction-setup.md`
- Figma 파일: https://www.figma.com/design/dopi19k96TB1E0iTnpyt7o

## 2. 핵심 방향

라이어게임은 온라인 멀티플레이 앱이 아니라, 오프라인 모임에서 핸드폰 하나를 돌려가며 쓰는 앱이다.

따라서 디자인의 우선순위는 다음 순서다.

1. 빠르게 시작된다.
2. 진행자가 덜 말해도 된다.
3. 다음 사람이 무엇을 해야 하는지 분명하다.
4. 결과 화면이 웃음과 반전을 만든다.
5. 반복 플레이가 귀찮지 않다.

## 3. 디자인 톤

추천 톤은 **밝은 파티 미스터리**다.

- 너무 어둡고 무거운 추리 게임처럼 보이면 첫 사용자가 부담을 느낀다.
- 너무 귀엽기만 하면 라이어게임의 긴장감이 약해진다.
- 밝은 포인트 컬러, 라이어 캐릭터, 큰 버튼, 짧은 문장으로 파티 게임 감각을 만든다.

유지할 요소:

- 큰 카드
- 강한 주요 CTA
- 라이어 캐릭터/피노키오 계열 이미지
- 투표 결과와 라이어 공개의 긴장감

피할 요소:

- 첫 화면을 설정 폼처럼 보이게 만드는 긴 옵션 목록
- 너무 파스텔톤이라 눌러야 하는지 애매한 버튼
- 결과를 점수표부터 보여주는 흐름
- "짜잔" 같은 효과를 텍스트만으로 처리하는 방식

## 3-1. React Bits 참고 방향

React Bits는 그대로 복사할 대상이 아니라, 라이어게임의 인터랙션 감각을 높이는 참고 자료로 사용한다.

참고할 감각:

- `Magnet`: 주요 버튼이 손가락을 끌어당기는 듯한 느낌
- `Profile Card`: 카드가 살짝 기울어지는 3D tilt와 glow
- 3D/card/shader 계열 컴포넌트: 평면 UI보다 깊이감 있는 게임 오브젝트

라이어게임에 맞게 번역하는 방식:

- 인원수 플러스/마이너스는 단순 그라데이션 버튼이 아니라 눌리는 3D 물리 버튼처럼 만든다.
- 후보 카드와 결과 리캡 카드는 살짝 떠 있는 카드처럼 그림자와 깊이를 준다.
- 라이어 공개/결과 화면은 glow, tilt, 캐릭터 이미지를 사용해 앱 화면이 아니라 게임 이벤트처럼 보이게 한다.
- 실제 구현에서는 과한 WebGL보다 CSS 3D, transform, shadow, short motion을 우선한다.

## 4. 첫 화면 기준

첫 화면은 설정 화면이 아니라 시작 화면처럼 보여야 한다.

기본 노출:

- 오른쪽 상단의 작은 언어 선택
- 앱 제목과 짧은 설명
- 인원수 선택
- 큰 `빠른 시작` 버튼
- `게임방법`
- 접힘 형태의 `이름 입력`
- 접힘 형태의 `고급 설정`

고급 설정에 들어갈 항목:

- 라이어 수
- 난이도
- 라운드 수
- 타이머
- 카테고리/단어팩
- 카테고리 힌트 보기

인원수 조절 버튼은 명확히 누르고 싶게 보여야 한다. 크기, 그림자, 눌림 모션, 강한 대비가 필요하다.

플러스/마이너스 버튼 기준:

- 상단 face와 아래 depth가 분리되어 보이는 3D 버튼
- `-`는 Main 계열, `+`는 Sub 계열로 역할을 구분
- 눌렀을 때 Y축으로 내려가며 depth가 줄어드는 모션
- 흰색 gloss, 진한 하단 그림자, 충분한 크기
- 단순한 파스텔/그라데이션 캡슐 버튼 금지

버튼 시스템 확장 기준:

- `빠른 시작`, `카드 보기`, `투표 저장`, `다음 라운드` 같은 주요 CTA도 같은 3D 물리 버튼 계열로 맞춘다.
- 버튼 색은 단색을 기본으로 하고, 그라데이션은 최소화한다.
- 입체감은 색 전환이 아니라 하단 depth, 상단 gloss, inset shadow, press motion으로 만든다.
- 보조 버튼과 접힘 버튼도 완전히 평면으로 두지 말고 얕은 depth를 준다.
- 선택된 후보 카드도 3D 버튼처럼 떠 있고 눌린 대상으로 보여야 한다.
- 모든 주요 버튼을 빨강으로 만들지 않는다. 빨강은 라이어, 위험, 공개, 강한 판정에 제한한다.
- 진행/확인/저장은 청록, 빠른 시작은 오렌지, 선택/힌트/리캡은 앰버를 기본으로 사용한다.

버튼 색 역할:

- Main Red `#E11D48`: 라이어, 위험, 공개, 강한 판정
- Sub Teal `#0F766E`: 진행, 확인, 저장, 다음 단계
- Party Orange `#F97316`: 빠른 시작, 파티감, 가장 중요한 시작 CTA
- Accent Amber `#F59E0B`: 선택 상태, 힌트, 리캡, 가벼운 강조
- Light Surface `#FFFFFF/#FFF7ED`: 보조 버튼, 접힘 버튼, 설명성 액션

색 대비 기준:

- 밝은 배경 위에는 연한 하늘색/민트 텍스트를 쓰지 않는다.
- 밝은 Surface 위의 보조 강조 텍스트는 최소 `#0F766E` 수준의 진한 청록을 사용한다.
- 연한 민트/하늘색은 어두운 배경 위의 장식 또는 작은 포인트에만 사용한다.
- 사용자가 읽어야 하는 문구는 색보다 대비를 우선한다.

## 5. 게임 방법

`자세히 읽기`보다 `게임방법`이 맞다.

게임 방법은 토글로 본문이 길게 펼쳐지는 형태보다, 모달이나 새 창처럼 분리된 읽기 화면이 적합하다.

필수 설명:

- 시민과 라이어의 목표
- 카드 확인 방법
- 토론 방법
- 비밀투표 방법
- 라이어가 지목됐을 때 마지막 기회
- 동점 처리
- 점수와 최종 승리 방식

## 6. 카드 확인

카드 확인은 패스폰 흐름이어야 한다.

권장 흐름:

1. `1번 차례입니다. 혼자 카드 확인을 누르세요.`
2. 카드 확인
3. 화면 가림
4. `다음 사람에게 넘겨주세요.`
5. 다음 사람 확인

중요 기준:

- 시민 카드와 라이어 카드는 화면 색만으로 구분되지 않아야 한다.
- 라이어 카드에 카테고리 힌트가 켜져 있으면 카테고리만 보여준다.
- 카테고리 힌트가 꺼져 있으면 라이어에게도 카테고리를 보여주지 않는다.

## 7. 투표

투표는 표 수를 입력하는 관리 UI가 아니라, 한 사람씩 핸드폰을 돌리는 비밀투표여야 한다.

권장 흐름:

1. 현재 투표자 표시
2. 후보 선택
3. 자기 자신은 선택 불가
4. 투표 저장
5. 화면 가림
6. 다음 사람에게 넘기기
7. 마지막 투표 후 `투표 집계`

기본 공개 원칙:

- 라운드 중에는 누가 누구를 찍었는지 공개하지 않는다.
- 결과 화면에서는 득표 패턴과 최다 득표자만 보여준다.
- 상세 투표 내역은 최종 결과에서 선택 공개하는 방식이 적합하다.

## 8. 동점 처리

동점이 발생하면 앱은 내부적으로 라이어 포함 여부를 알 수 있지만, 사용자에게 바로 알려주면 안 된다.

동점 화면 선택지:

- `재투표하기`: 동점 후보만 놓고 다시 비밀투표
- `빠른 판정`: 동점 후보를 그대로 공동 지목 처리

규칙:

- 빠른 판정에서 동점 후보 안에 라이어가 있으면 라이어 지목 성공
- 동점 후보 안에 라이어가 없으면 라이어 승리
- 재투표에서도 동점이면 자동 빠른 판정으로 끝낸다

## 9. 라이어 지목과 마지막 기회

투표 결과 라이어가 지목되면 바로 정답이나 결과를 보여주지 않는다.

권장 흐름:

1. 투표 결과 공개
2. `라이어 지목 성공`
3. 지목된 라이어의 마지막 기회
4. 시민이 `맞혔어요` 또는 `틀렸어요` 선택
5. `지목된 라이어에게 넘겨주세요`
6. 라이어가 결과 확인
7. `맞습니다!` 또는 `틀렸습니다!`
8. `정답 확인하기`
9. 라운드 결과

이 흐름에서는 별도의 3,2,1 카운트다운을 넣지 않는다. 오프라인 진행 속도가 더 중요하다.

## 10. 결과 화면

결과 화면은 점수표보다 드라마가 먼저다.

권장 정보 순서:

1. 투표 결과
2. 판정
3. 라이어 공개
4. 제시어 공개
5. 라운드 하이라이트
6. 점수표
7. 다음 라운드 또는 최종 결과

이미지/연출 기준:

- 라이어 공개는 피노키오/라이어 캐릭터를 활용한다.
- `짜잔` 같은 문구만 넣는 대신 이미지와 모션으로 표현한다.
- `가장 의심받은 사람`, `억울한 시민`, `역전 성공 라이어` 같은 하이라이트는 텍스트만 나열하지 말고 아이콘/캐릭터/시각적 구분을 둔다.
- 안내 문구는 파스텔톤으로 흐리게 만들지 않는다.

## 11. 최종 결과

최종 결과는 점수표만 보여주면 약하다. 리캡과 반복 플레이 CTA가 필요하다.

추천 리캡:

- 오늘의 우승자
- 오늘의 의심왕
- 가장 억울한 시민
- 최고의 라이어
- 역전의 주인공

가장 큰 CTA:

- `같은 멤버로 한 판 더`

보조 CTA:

- `설정 바꾸기`
- `지난 기록`
- `투표 내역 보기`

## 12. 구현 체크리스트

- 첫 화면에서 빠른 시작 버튼이 첫 viewport 안에 보이는가?
- 언어 선택이 작고 명확하게 배치되어 있는가?
- 이름 입력과 고급 설정은 기본으로 접혀 있는가?
- 인원수 버튼은 누르고 싶게 생겼는가?
- 플러스/마이너스 버튼이 3D 물리 버튼처럼 보이는가?
- 모든 주요 CTA가 같은 3D 버튼 언어를 공유하는가?
- 버튼 그라데이션이 과하지 않고 단색+깊이감 중심인가?
- 빨강 버튼이 너무 많지 않고, 역할별 색이 분리되어 있는가?
- 밝은 배경 위 보조 문구가 충분히 진하게 보이는가?
- 카드 확인 후 다음 사람에게 넘기는 화면이 있는가?
- 투표가 패스폰 비밀투표로 진행되는가?
- 동점에서 라이어 포함 여부를 먼저 노출하지 않는가?
- 투표 집계 후 불필요한 3,2,1이 나오지 않는가?
- 라이어 지목 후 마지막 기회 흐름이 빠른가?
- 결과 화면에서 투표 결과, 판정, 라이어, 제시어, 하이라이트가 점수표보다 먼저 보이는가?
- 최종 결과에 같은 멤버로 다시 시작하는 큰 버튼이 있는가?
